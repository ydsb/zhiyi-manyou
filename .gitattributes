# 统一换行符，避免 Windows/Linux 协作时整文件 diff
* text=auto eol=lf

# Windows 脚本保持 CRLF（PowerShell 5.1 对 LF 的 .ps1 兼容性一般）
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 生成的迁移 SQL 标记为二进制：内容里含大量中文技能名与备注，
# 换行符转换只会制造无意义的大 diff（10-ontology-graph.sql 单文件 11,000+ 行）。
# 声明为 binary 后 git 不做任何规范化，文件内容在本机与同学机器上逐字节一致。
# 注意：无论仓库里存的是什么，都应当用 --default-character-set=utf8mb4 导入，
# 否则中文会乱码 —— 详见 scripts/README.md。
sql/10-ontology-graph.sql binary

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.pdf binary
*.doc binary
*.docx binary
*.xls binary
*.xlsx binary
*.zip binary
*.jar binary
*.woff binary
*.woff2 binary

# 语言统计时排除生成物
package-lock.json linguist-generated=true
*.min.js linguist-generated=true
