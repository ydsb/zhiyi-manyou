# 知驿·漫游 —— 项目进度总览

> 大学生创新训练计划项目 · 西北大学计算机学院
> 项目负责人：李泽宬（2024117420）　成员：吴禹晗、杨渡　指导教师：董乐红 副教授
> 最后更新：2026-09-17

**一句话现状**：后端 **Spring Boot 3.3.5 + JDK 17**，**M2 技能本体**、**M4 供需集市/交换状态机**、
**M5 协作工作台**、**M6 双向互评存证**、**M7 数字档案与能力画像**、**M8 信用体系与社区治理**、**M9 管理后台**七个模块已完成（**178 项单元测试全通过**，AC-02/AC-05/AC-06/AC-07/AC-08 均已实测），
前端技能图谱、供需集市、我的交换、协作工作台四页已接入真实接口。

---

> 📌 **换会话/换人接手时，先读 `docs\交接说明.md`** —— 里面有当前状态、
> 下一步任务（M7）、环境限制的应对方式，以及全部踩坑的速查表。

---

## 一、总体阶段

| 阶段 | 内容 | 状态 |
|------|------|------|
| S1 | 需求工程：需求规格说明书、原型 | ✅ 需求文档完成（`知驿·漫游-需求文档.md`） |
| S2 | 核心架构与数据底座：后端主干 + 前端 | ✅ **完成**（M1/M2/M4/M5/M6/M7/M8/M9，M3 关键词降级版） |
| S3 | NLP 语义引擎 + 推荐算法（Python + 向量库） | 🔄 进行中（语义引擎已完成并实测；预训练模型/ES 受环境限制） |
| S4 | 封闭测试与试点（≥3 学院）、压测、安全加固 | ⬜ 未开始 |
| S5 | 结项：论文、软著、结题材料 | ⬜ 未开始 |

---

### S3 语义引擎（✅ 已完成可实现部分）

| 项 | 状态 |
|------|------|
| 中文 n-gram 分词（无需词典） | ✅ `nlp/tokenizer.py` |
| TF-IDF 向量化（IDF 预先拟合） | ✅ `nlp/tfidf.py` |
| 稠密语义向量（随机化截断 SVD） | ✅ `nlp/embedder.py` |
| 向量检索（精确 KNN + 可选 LSH） | ✅ `nlp/index.py` |
| 图谱关系加成（可解释） | ✅ 检索阶段显式加成 |
| FastAPI 算法服务 | ✅ `serve.py`，7 个接口 |
| Java 侧集成 + 熔断降级 | ✅ `SemanticMatchClient` |
| 单元测试 | ✅ 46 项全通过 |
| **技能本体扩充** | ✅ 788 条 / 62 二级学科（原 14 条 / 8 个） |
| **AC-03 准确率评测** | ✅ Hit@3 **93.3%**、Hit@1 73.3%、MRR 0.825（30 条标注集） |
| **预训练语义模型** | ❌ 装不了 torch / HuggingFace 超时 → 用 LSA 替代 |
| **Elasticsearch HNSW** | ❌ 无 Docker → 用 numpy 精确检索替代 |

**实测**：检索 11 ms、跨表述匹配 0.5908、图谱加成 +0.30、相似度判定 0.9912。

**两个实测结论**（详见 docs/S3环境说明.md 第六节）：
1. 图谱关系**不能拼进训练语料**（IDF 机制会使相似度下降），只能在检索阶段加成；
2. LSA **无法凭空建立联系**（无共享词时向量正交），多路召回是必需的而非可选。
## 二、后端（`zhiyi-manyou/`）

**技术栈**：Spring Boot 3.3.5 · JDK 17 · MyBatis-Plus 3.5.7 · MySQL 8 · JWT · Spring Security 6

### 已完成

| 项 | 说明 |
|----|------|
| 工程骨架 | Maven 多模块：`zy-common` + `zy-server`，57 个 Java 文件 |
| 数据库 | 15 张表，`sql/01-schema.sql`（幂等），已导入 `zhiyi_manyou` 库 |
| 领域模型 | 14 个实体 + 10 个 Mapper（含交换状态机、互评 SHA-256 存证） |
| 基础设施 | 统一响应体、六段错误码、全局异常处理、traceId 链路追踪、Jackson 时序 |
| 认证鉴权 | JWT 双令牌 + Spring Security 6 无状态授权 + BCrypt |
| 接口 | `/api/auth/*`（登录/注册/刷新/me/登出）、`/api/health`、`/api/health/info` |
| 质量 | `smoke-test.ps1` **27/27 项全部通过** |

### 已完成的关键修复（迁移与调试中定位）

1. 枚举常量实参不支持前向引用 → 改用 `switch` 在方法体内定义状态流转规则
2. `@TableField(typeHandler=...)` 对 MyBatis **自动结果映射无效** → 统一改为枚举名 VARCHAR 存储
3. `new ObjectMapper()` 绕过 JSR-310 模块导致 `LocalDateTime` 序列化失败 → 改为注入容器实例
4. 返回非 200 时容器转发 `/error`（无错误页）导致响应体丢失 → 统一 HTTP 200 + code
5. Spring Boot 3 迁移：`javax.*` → `jakarta.*`、Security Lambda DSL、`EnableMethodSecurity`、`mybatis-plus-spring-boot3-starter`
6. `zy-common` 不应执行 `spring-boot:repackage` → 从该模块移除插件声明
7. **循环依赖**：`SkillServiceImpl` ⇄ `SkillGraphServiceImpl` → 图谱服务改为直接依赖 `SkillMapper`
8. **Spring Security 6 的 ERROR 分发**：默认对所有 dispatcher 类型生效，会把内部错误转发拦成 401 → `shouldFilterAllDispatcherTypes(false)`
9. **解析算法最长匹配缺陷**：原先用 `indexOf` 只取首次出现，同一标签多次出现时会选错 → 改为「首字符索引 + 最长匹配优先」
10. **意图判定距离缺陷**：原先按规则顺序返回首个命中，单字「会」会抢走更靠后的「需要」→ 改为取离技能词最近的关键词，并给规则分级设最小长度

### M2 技能本体模块（✅ 已完成）

| 需求 | 内容 | 落地 |
|------|------|------|
| FR-M2-01 | 自然语言发布供给/需求 | `POST /api/skills/parse` |
| FR-M2-02 | 抽取 (主体,动作,技能实体) 三元组 | `SkillTextParser`，38 项单元测试 |
| FR-M2-03 | 映射标准化标签 + 可解释依据 | 返回 `matchType` 与 `reason`，前端可回显纠正 |
| FR-M2-04 | 三层分类体系 | `GET /api/skills/tree` |
| FR-M2-05 | 知识图谱（先决/互补/同义） | `zy_skill_ontology` + `GET /api/ontology/graph` |
| FR-M2-06 | 图谱游走挖掘隐性互补需求 | `SkillGraphService.augment()`，补全标签打 0.85 折扣 |
| FR-M2-07 | 同义词库跨域映射 | `zy_skill.alias`，SQL 重跑会同步更新别名 |
| FR-M2-08 | 图谱可视化数据 | 前端「跨学科关系」面板 + 节点活跃度 |

**验收对照**：AC-02（20 例口语化描述抽取准确率）→ **实测 19/20 = 95%**，基线 70%。

**已知不足**：种子标签仅 12 个（目标 1000+）。算法的骨架已就绪，
后续是**数据填充工作**（可由 S1 调研产出的学科术语表批量导入）。

**图谱边已补齐（2026-09-22）**：原先只有 3 条示例边，图谱页几乎空白、
`augment()` 的隐性需求挖掘基本不触发。现由 `nlp-service/gen_graph.py` 生成
`sql/10-ontology-graph.sql`，实测入库 **593 条边**：

| 指标 | 数值 |
|---|---|
| COMPLEMENT（互补协作，无向） | 424 |
| PREREQUISITE（先决条件，有向） | 151 |
| SYNONYM（同义映射） | 18 |
| 跨门类边占比 | 36.3%（215/593） |
| 被连接到的技能 | 556 / 788（70.6%） |

生成器**不做任何自动推断**（共现统计与 embedding 近邻都会产出似是而非的边：
按余弦相似度"篮球技战术训练"和"足球技战术训练"必然最靠前，但那条边毫无价值），
全部边为人工判读并逐条写明"这两种技能为什么会一起出现"。技能名逐字校验，
校验不通过直接退出 —— 实测这道闸拦下了 **189 个凭印象写错的技能名**。
验收脚本：`scripts/verify-graph.ps1`（16 项检查全绿）。

### M4 供需集市与交换状态机（✅ 已完成）

| 需求 | 内容 | 落地 |
|------|------|------|
| FR-M4-01 | 发布需求卡片（标题/详述/期望技能/时长/时段/可见范围） | `POST /api/demands`，`zy_demand` |
| FR-M4-02 | Issue 式信息流 + 多维筛选排序 | `GET /api/demands`（MATCH/LATEST/HOT 三种排序） |
| FR-M4-03 | 高匹配卡片自动高亮置顶 | `MatchScoreCalculator`，阈值 0.75，前端橙色描边+角标 |
| FR-M4-04 | 交换状态机 | `ExchangeStatus` 七状态 + 合法后继校验，非法流转返回 3007 |
| FR-M4-05 | 邀约 / 接受 / 拒绝 / 撤回 + 协作空间 | `POST /api/exchanges/apply`、`/interests/{id}/accept` 等 |
| FR-M4-06 | 以技易技的双向置换 | `giveSkillId`/`learnSkillId` 对齐 `expectedSkillId`/`offerSkillId` |
| FR-M4-07 | 内容审核（敏感词 + 广告/刷单） | `DemandContentAuditor`，驳回/转人工复核两级 |
| FR-M4-08 | 收藏与订阅推送 | ⬜ P2，本轮未做 |
| FR-M4-09 | 并发交换上限（默认 5） | 占用配额的状态为洽谈中/进行中，超限返回 3008 |

**匹配度算法**（本模块的核心）：不是单向关键词命中，而是
**「我能帮上他」×「他能回报我」** 双向评估，五个加权因子全部返回得分与自然语言依据。
实测：李泽宬（擅长动效/急需数学建模）看吴禹晗的卡片（急需动效/可提供数学建模）
得 **0.91 且判为高匹配**，另外两张只有"同二级学科"命中的得 0.56 且不置顶。

**数据**：`zy_demand` 4 张种子卡片 + `zy_user_skill_profile` 三份演示画像
（画像必须存在，否则匹配度恒为 0，集市排序看不出效果）。

### M5 协作工作台（✅ 已完成）

| 需求 | 内容 | 落地 |
|------|------|------|
| FR-M5-01 | 专属协作空间，仅双方可见 | 所有工作台接口过 `requireParticipant`，非参与方返回 3014；下载接口同样受控 |
| FR-M5-02 | 任务拆解与打卡 | `zy_collab_task` + **新增 `assignee_sno`**（个人维度的前提）；逾期自动判定 |
| FR-M5-03 | 文件版本传输与回溯 | `zy_collab_file`：`group_key` 逻辑分组 + `version` 自增，历史版本保留可回溯 |
| FR-M5-04 | 时间轴视图 | `zy_collab_event` 统一事件表，标题服务端渲染好并附图标 |
| FR-M5-05 | 站内消息与通知中心 | `NotificationService` + 顶栏红点；打卡提醒由定时任务扫描 |
| FR-M5-06 | **过程性评价语料** | `process-summary` 接口，含个人维度明细与一句话小结 |
| FR-M5-07 | 在线沟通（文字 + 附件） | `zy_collab_message`，留言永久留存作为争议证据 |
| FR-M5-08 | 阶段性成果互相确认 | 🔶 部分覆盖（任务打卡 + 证据地址），独立"共同确认"流程留待 M6 与互评一起做 |

**设计要点（值得在结项材料里强调）**：

1. **FR-M5-06 是创新点 1 的数据来源**。追溯矩阵里创新点 1 明确挂着 FR-M5-06，
   所以这里没有只做 CRUD，而是产出**客观行为语料**（完成率、按期率、拆解粒度、
   启动速度、停滞时长、个人明细）。它与 M6 互评并列呈现以互相印证，
   这正是"减少凭印象打分与互刷好评空间"的技术落点。
2. **只给原始计数，不给综合评分**。加权口径要与 M6 的评价维度对齐并落库为
   不可篡改记录（FR-M6-03），在此处算分会出现两套口径、无法追溯。
3. **存储介质策略化**（FR-M9-06）。`FileStorage` 接口 + `LocalFileStorage` 实现，
   采购云服务后加 `OssFileStorage` 即可切换，业务代码零改动。
4. **逾期只能靠定时任务**。逾期是时间流逝产生的事实，没有用户动作可触发，
   因此 `CollaborationReminderJob` 每 30 分钟扫描（幂等，不重复骚扰）。

### M6 双向互评与哈希存证（✅ 已完成）

| 需求 | 内容 | 落地 |
|------|------|------|
| FR-M6-01 | 待互评状态下双方须完成互评 | `POST /api/evaluations`，提交即封存；双方齐备自动流转「已完成」 |
| FR-M6-02 | 四维度打分 + 文字评语 | `EvaluationDimension` 枚举定义维度与权重，`DimensionScoreCalculator` 统一算总分 |
| FR-M6-03 | 提交后哈希固化，仅可追加修正 | `EvaluationGrade.seal()` + `zy_evaluation_amendment`（追加式，不覆盖历史） |
| FR-M6-04 | 存证校验 | `GET /api/evaluations/verify?recordNo=` + `GET /api/certificates/{code}`（匿名验真） |
| FR-M6-05 | 双方完成前不可见对方评分 | 服务端置 null 后返回（绕前端也拿不到） |
| FR-M6-06 | 超时提醒 + 默认计分 | `EvaluationTimeoutJob`：3 天提醒、7 天默认计分（80 分 + `timeout_flag` 标识） |
| FR-M6-07 | 互刷信用检测 | `CollusionDetector` 四模式，命中标记待人工复核而不拦截 |

**AC-05 实测结果**（这是结项演示最硬的技术点，已逐项验证）：

| 场景 | 结果 |
|------|------|
| 基线（未篡改） | `intact=True`，0/2 异常 ✅ |
| 篡改维度分 / 评语 / 总分 / 评价人 | **全部检出** ✅ |
| 逐项还原后 | 回到 `intact=True` ✅ |
| 无效校验码 | 返回 3056 ✅ |
| 互刷检测 | 自动命中"进入待互评 0 分钟即评价"并标记 `PENDING` ✅ |

**设计要点（值得在结项材料里强调）**：

1. **总分口径唯一**：权重集中定义在枚举里，总分只由一个计算器产出 ——
   否则能力画像（M7）与信用计算（M8）会出现"同一评价不同分数"且无法追溯。
2. **参与哈希的字段必须可逐字节重现**（踩坑后的修正）：
   `dim_scores` 是 MySQL JSON 列，读写往返会改格式，**不能参与哈希**；
   另存 `dim_canonical` 规范串专用于存证。首版没意识到这点，
   导致**未篡改的记录也被误报为篡改**（基线 2/2 异常）。
3. **如实说明能力边界**：只做哈希固化时，有库权限者可连哈希一起重算。
   因此存证分级（`HASH`/`TIMESTAMP`/`CHAIN`），对外统一表述"可检出篡改"
   而非"物理上不可篡改"，校验接口会把等级与边界说明一并返回给验真方。

### M7 数字档案与能力画像（✅ 已完成）

| 需求 | 内容 | 落地 |
|------|------|------|
| FR-M7-01/02 | 登录后主页的数字档案 + 五维雷达图 | `GET /api/profile/radar`，维度与映射定义在 `AbilityDimension` 枚举 |
| FR-M7-03 | 批处理汇总协作时长与质量得分 | `ProfileBatchJob` 每日刷新当月快照（`zy_ability_profile`） |
| FR-M7-04 | 数字勋章自动授予 | 解析 `condition_expr`，读取画像时按需评估（幂等） |
| FR-M7-05 | 智能成长周报 | `zy_growth_report`，每周一生成并推送（复用 M5 通知中心） |
| FR-M7-06/08 | 一键导出带验证码的鉴定报告 | 结构化数据 + 前端排版打印为 PDF，带 M6 校验码可匿名验真 |
| FR-M7-07 | 成长轨迹时间线 | `GET /api/profile/growth`，读快照序列画历史曲线 |

**实测结果**（李泽宬账号，4 次已完成协作）：

| 维度 | 得分 | 出处 |
|---|---|---|
| 工程逻辑 | 88.75 | ECharts 数据可视化（工学）× 2 次 / 6 小时 |
| 艺术审美 | 86.25 | 视频剪辑 86.25；UI/UX 设计 6.5 小时但**对方未评**，如实标注不计分 |
| 沟通协作 | 90.67 | 3 次互评的沟通效率维度分 |
| 数据分析 / 人文表达 | 无数据 | 返回 null 而非 0，前端画虚线并标注 |

**三个关键设计决策**（都有踩坑背景，详见 README §6.13）：

1. **雷达图实时算、快照定时落库**：实时算保证 AC-06「随协作数据变化正确刷新」；
   快照用于画历史曲线（实时算给不出历史值）。两者共用同一个计算器，口径天然一致。
2. **只按"我提供的技能"归类**：教别人什么才证明我会什么。若按"涉及的全部技能"，
   雷达图会虚高失真。
3. **无样本返回 null 而非 0**：0 分会被读成"能力很差"，事实是"没有数据"。

### M8 信用体系与社区治理（✅ 后端已完成）

| 需求 | 内容 | 落地 |
|------|------|------|
| FR-M8-01 | 信用值多因子模型 | `CreditCalculator` 五因子加权（履约 0.30 / 互评 0.30 / 申诉 0.20 / 活跃 0.10 / 及时性 0.10），因子明细可追溯 |
| FR-M8-02 | 等级分档 + 权限关联 | `CreditLevel` 五档；等级直接改写 `exchange_quota`，被 M4 邀约校验读取；「优秀」以上才可任仲裁委员 |
| FR-M8-03 | 争议申诉与证据 | `zy_dispute` 扩展类型/陈述/答辩/表决时限；附件与陈述至少一项 |
| FR-M8-04 | 仲裁委员会 + 卷宗 | 抽取跨学科高信用委员；卷宗**一次生成即快照**（任务/沟通/交付/互评完整性/时间轴） |
| FR-M8-05 | 匿名投票集体决议 | 投票内容哈希存证；公示仅票数分布；平票不作责任认定 |
| FR-M8-06 | 裁决自动执行 | 调信用、交换置争议态、累计 3 次败诉自动封禁 |
| FR-M8-07 | 规则公示 + 操作审计 | `/api/governance/rules`、`/logs`、`/statistics` 均匿名开放；`zy_governance_audit` 只追加 |
| FR-M8-08 | 管理员紧急干预留痕 | 理由必填；`ADMIN_INTERVENE` 动作 + 公示；隐私操作 `visible=0`（留痕不公示） |

**AC-07 实测结果**（端到端跑通）：

| 场景 | 结果 |
|------|------|
| 信用随履约/评价变动 | ✅ 150→135（败诉）、100→103（胜诉补偿） |
| 完整申诉流程 | ✅ 申诉 → 答辩 → 卷宗 → 投票 → 裁决 |
| 委员抽取 | ✅ 3 名跨学院高信用委员；**不足时转管理员兜底，未降级凑数** |
| 匿名表决 | ✅ 2:1 表决；公示仅 `{total:3, forApplicant:2, forRespondent:1}` 不含投票人 |
| 裁决自动执行 | ✅ 信用调整落地、交换置争议态 |
| 治理公示与审计 | ✅ 匿名可查；管理员视角可见全部 |

**踩到的两个缺陷（都已修 + 加回归测试）**：

1. **信用值域与等级阈值不自洽**（严重，静默算错）：`adjust()` 把值钳在 0~100，
   而等级表定义「优秀 140~179」。用户 150 分被扣 15 分时被钳成 100 —— **惩罚被吞掉 35 分**；
   补偿 +3 在封顶时完全丢失。根因是"值域上限"与"等级阈值"两处约束没对齐。
   现统一由 `CreditLevel.RANGE_MAX`（=200）表达，`CreditRangeTest` 锁定两个场景。
2. **`credit_level` 存储约定不一致**：DDL 注释说 1~5，`CreditService` 写 `ordinal()`（0~4），
   `AuthService` 写死 1。现已统一收敛到 `CreditLevel.levelCode()`，并让重算无条件校正历史脏数据。

### M9 管理后台（✅ 已完成）

| 需求 | 内容 | 落地 |
|------|------|------|
| FR-M9-01 | 用户管理 | `/api/admin/users`：查询/禁用启/角色分配/实名核验/手动重算信用；**自我保护 + 留痕强制** |
| FR-M9-02 | 标签与图谱维护 | 技能 CRUD（M2）+ 批量导入导出 + `zy_skill_import_batch` 批次回溯 |
| FR-M9-03 | 内容审核队列 | 待审卡片 + 用户举报（`zy_content_report`），举报成立按类型自动处置 |
| FR-M9-04 | 运营数据看板 | 日活/新增/交换量/匹配成功率/学科与信用分布 + `zy_dashboard_snapshot` 日快照趋势 |
| FR-M9-05 | 行为日志采集 | 表已建（`zy_interaction_log`），消息队列削峰链路待 S4 |
| FR-M9-06 | 数据备份与恢复 | 见下方"运维"说明 |
| FR-M9-07/08 | 开放能力 / Tool Calling | `GET /api/tools`（JSON Schema）+ `POST /api/tools/call`，**约定参考 MCP** |

**AC-08 实测**：用户管理四类操作可用且都要填说明；操作自己→3091、不填说明→3093；
待审卡片与举报队列可用；看板指标与分布正常；技能导入 5 行中新增 2/更新 1/失败 2（错误行列出原因）；
开放工具 4 个带 schema，调用成功，缺参数与未知工具均给明确错误。

**三条管理端设计原则**：

1. **自我保护**：不能对自己禁用/降权（防把自己锁在门外），不能操作其他管理员（防管理员互相封禁）——
   这两条是管理后台最常见的生产事故来源。
2. **留痕强制**：所有写操作必须填说明，写入治理审计。实名核验涉及身份信息 → `visible=0`（留痕不公示）。
3. **逐行校验而非整批回滚**：技能导入跳过错误行并列出原因，正确行照常导入。

**M9-06 数据备份说明（2026-09-22 已实现，取代此前的"仅部署配置"说法）**：

原先这里写的是"未实现应用内备份调度，属于部署配置而非代码，因此不在仓库内"。
那个说法**掩盖了一个真实风险**：仓库里连一个可执行的备份脚本都没有，
"配置"从未被任何人写下来，等于备份完全靠自觉。现已补齐为可运行工具：

- **脚本**：`scripts/backup.ps1`（四个动作：`backup` / `list` / `verify` / `restore`）
- **说明**：`scripts/README.md`（含参数表、产出格式、踩过的 6 个坑）
- **幂等与安全**：导入前强制校验 SHA256；篡改一个字节即拒绝恢复；
  覆盖已有数据的库必须显式加 `-Force`；默认保留最近 14 份
- **实测**：27 张表 / 254,774 字节原始 SQL / 压缩后 58 KB / 354 ms；
  恢复到临时库后 27 张表行数全一致，`zy_skill`、`zy_skill_ontology`、
  `zy_user_skill_profile` 三张表**内容 MD5 与源库相同**，中文无乱码

仍属部署层、未纳入代码的部分：MySQL binlog 开启、异地存放、定时任务。
建议用「任务计划程序」每日调用 `scripts/backup.ps1 backup`。

**恢复演练务必真做一次**："能备份"和"能恢复"是两件事，只备份不恢复，
等于把风险从"删库"换成了"以为有备份"。演练步骤见 `scripts/README.md`。

### 待开发（S2 业务功能）

| 模块 | 需求编号 | 状态 |
|------|----------|------|
| ~~M2 技能本体与知识图谱~~ | FR-M2-01 ~ 08 | ✅ **已完成** |
| M3 智能匹配与推荐 | FR-M3-01 ~ 08 | 🔶 关键词降级版 + 集市匹配度已具备，向量检索待 S3 |
| ~~M4 供需集市与交换状态机~~ | FR-M4-01 ~ 09 | ✅ **已完成**（FR-M4-08 收藏/订阅为 P2，未做） |
| ~~M5 协作工作台与过程记录~~ | FR-M5-01 ~ 08 | ✅ **已完成**（FR-M5-08 独立确认流程留待 M6） |
| ~~M6 双向互评与不可篡改存证~~ | FR-M6-01 ~ 07 | ✅ **已完成** |
| ~~M7 数字档案与能力画像~~ | FR-M7-01 ~ 08 | ✅ **已完成**（FR-M7-05 周报需定时任务触发才有数据） |
| ~~M8 信用体系与社区治理~~ | FR-M8-01 ~ 08 | ✅ **已完成** |
| ~~M9 管理后台~~ | FR-M9-01 ~ 08 | ✅ **已完成**（M9-05 采集链路待 S4） |

---

## 三、前端（`zhiyi-manyou-web/`）

**技术栈**：Vue 3.5 · Vite 8 · TypeScript 5.7 · Pinia 4 · Vue Router 5 · Element Plus 2.14 · ECharts 6

### 已完成（框架级）

| 项 | 状态 | 验证方式 |
|----|------|----------|
| 工程脚手架与构建 | ✅ | `npm run build` 通过，2273 模块，代码分割生效 |
| 路由与登录守卫 | ✅ | 未登录访问受保护页面自动跳登录 |
| 首次登录强制导引 | ✅ | 实测 admin 登录后跳到 `/onboarding` |
| 请求层（令牌/解包/401） | ✅ | 登录请求经代理到达后端并成功 |
| 认证状态管理 | ✅ | 登录 → 跳转 → 顶栏显示用户信息 |
| 登录页 | ✅ | 已截图验证 |
| 新手导引 + 技能星图 | ✅ | 三步选择实测走通，星图实时渲染（见下方"导引页补齐"） |
| **新手导引（已接入真实接口与落库）** | ✅ | 788 个真实标签 + NLP 双通道抽取 + 保存落库，首登死循环已修复 |
| 数字档案（ECharts 雷达图 + 像素勋章） | ✅ | 已截图验证 |
| 供需集市（Issue 式卡片） | ✅ | 已截图验证 |
| **技能图谱（已接入真实接口）** | ✅ | 标签树 / 图谱关系 / 解析试玩全部走通 |
| **供需集市（已接入真实接口）** | ✅ | 发布 / 匹配度拆解 / 详情抽屉 / 发起邀约 |
| **我的交换（已接入真实接口）** | ✅ | 状态机按钮由后端合法后继驱动、邀约响应、撤回、工作台入口 |
| **协作工作台（已接入真实接口）** | ✅ | 任务打卡 / 文件版本回溯 / 留言 / 时间轴 / 过程指标 五个标签页 |
| **通知中心（顶栏红点）** | ✅ | 未读计数、单条与全部已读、点击跳转 |
| 个人中心（资料 + 技能画像 + 勋章 + 档案） | ✅ | 技能画像与勋章已接入真实接口，报告导出可用 |
| 404 页面 | ✅ | — |

### 导引页补齐（原为 M1 骨架，存在空转缺陷）

排查"新手漫游导引里选项好像有点少"时发现的真实缺陷，已全部修复：

| 项 | 原状态 | 现状 |
|----|--------|------|
| 标签池 | 12 个硬编码字符串 | 788 个真实标签（来自 `/api/skills/tree`） |
| 搜索/门类筛选 | 无 | 跨门类搜索 + 12 门类筛选 |
| 自然语言抽取 | 无（提示"S3 阶段上线"） | 规则词典 + 语义向量双通道合并 |
| 选择是否落库 | ❌ 只有一句 TODO | ✅ `POST /api/profile/skills`，实测写入 6 行 |
| 首登死循环 | ❌ 画像恒空 → 每次登录都被拉回导引页 | ✅ 保存后 `firstLogin` 变 false |
| 二次编辑 | ❌ 只有首登能设 | ✅ 个人中心「编辑画像」入口 |

关键设计取舍（详见 `SkillProfileServiceImpl` 类注释）：

- 保存语义是**整体覆盖自评部分**而不是增量追加，否则用户永远删不掉选错的标签；
- 但 `PEER` 互评 / `COURSE` 课程来源的记录**不参与删除** ——
  那些分数是协作成果累积出来的，不能因为用户改一次自评就清空。
  实测已锁定：提交时互评记录（PEER/88.50）完整保留。

### 已无遗留占位（原先的"等后端接口"三项已全部落地）

| 项 | 原状态 | 现状 |
|---|---|---|
| 数字档案雷达图 | 文档写"页面待接"（实际已接，描述过期） | ✅ 仪表盘实时渲染 7 维雷达 + 能力版图 |
| 个人资料保存 | `PUT /api/profile` 未实现，按钮弹"待开发" | ✅ 已实现（`a0338c1`），含三态语义与 5 项单测 |
| 我的数据导出 | FR-M1-07 未实现，按钮弹"待开发" | ✅ 已实现（`ddf3226`），九板块 JSON，含隐私边界规则 |

**目前前端已无任何"接口待开发"提示。**

### 仍未实现的功能（截至 2026-09-22）

| 条目 | 优先级 | 现状 |
|---|---|---|
| FR-M4-08 收藏 / 订阅同类需求推送 | P2 | 未做，无对应表 |
| ~~FR-M5-08 阶段性成果独立双向确认~~ | ~~P1~~ | ✅ **已完成**（双方确认 + 撤回清确认，9 个单测覆盖） |
| FR-M9-05 行为日志采集 | P1 | `zy_interaction_log` 表已建但 0 条，消息队列链路未接 |
| ~~FR-M9-06 数据备份与恢复~~ | ~~P0~~ | ✅ **已完成**：`scripts/backup.ps1` + 恢复实测通过 |
| Q5 证据锚定 | — | `evidence_level` 停在 `HASH`，无可信时间戳/联盟链 |
| 技能本体 ≥1000 条 | — | 当前 788 条（差 212） |
| ~~知识图谱关系~~ | ~~P1~~ | ✅ **已完成**：3 → **593 条边**，跨门类占 36.3% |
| `zy_skill_course_mapping` | — | 表空（0 条） |
| 前端自动化测试 | P1 | **零覆盖**，未装 Vitest，前端全靠手工验证 |
| S4 灰度 / 压测 / 安全加固 | P2 | 未开始 |
| 预训练语义模型 / Elasticsearch HNSW | — | 受环境限制，当前 TF-IDF+LSA + 内存精确 KNN 已达标 |

**图谱补全排序缺陷（2026-09-22 已修）**：`augment()` 原先只按边权重排序取前 5 个。
本图谱里绝大多数边权重只有 0.8/0.9 两档，一个种子技能的邻居几乎全部并列，
于是"按权重取前 N 个"退化成**任意取 N 个** —— 实测「我要做一个前端页面」
会补出「SolidWorks 三维建模」这种噪音。改为按
**(被几个不同种子连到, 权重, ID)** 三级排序：能被多个已命中技能共同指向的标签
更切题，ID 兜底保证结果稳定可复现。

**仍未解决的问题（如实记录）**：命中标签里若混入弱相关项（上例中的
「机械制图与公差配合」以 0.3093 侥幸进了 SEMANTIC），图谱会沿着它扩散出
机械类邻居。这是**语义召回的分辨率问题，不是图谱问题** ——
图谱只是忠实地把"机械制图 ↔ SolidWorks"这条真实存在的边报了出来。
根因在 `SEMANTIC_MIN_SCORE`（0.30）偏低导致弱命中进入种子集，
调高阈值会同时损失召回，属于需要评测集才能定的取舍，故此处不动。

---

## 四、环境与工具

| 组件 | 位置 / 版本 | 用途 |
|------|-------------|------|
| JDK 17 | `G:\DEV\jdk17`（Temurin 17.0.20.1） | **当前后端** |
| JDK 11 | `G:\DEV\jdk11` | 旧版后端（已升级，保留备用） |
| Node.js | `G:\Nodejs` v24.19.0 | 前端 |
| Maven | IDEA 自带 `maven3` 3.9.16 | 后端构建 |
| Maven 本地仓库 | `G:\DEV\.m2repo` | 工作区内，避免写用户目录 |
| npm 缓存 | `G:\DEV\.npm-cache` | 同上 |
| MySQL | `G:\DEV\mysql` 8.0.46 | 数据库 `zhiyi_manyou` |
| Maven 备用设置 | `G:\DEV\.mvn-settings-central.xml` | 阿里云镜像缺构件时直连 Central |
| Java 下载器 | `G:\DEV\_tools\Download.java` | 绕过 PowerShell schannel 限制下载文件 |

---

## 五、快速启动

```powershell
# ---------- 后端（端口 8080） ----------
. .\backend\build-env.ps1
zy-build
powershell -ExecutionPolicy Bypass -File backend\run-server.ps1
powershell -ExecutionPolicy Bypass -File backend\smoke-test.ps1

# ---------- 前端 ----------
cd frontend
npm run build      # 产出 dist/（生产产物）
npm run preview    # 用生产产物提供服务（端口 5173）

# 仅开发调试时才用：npm run dev（首次加载慢，见下方说明）
```

浏览器打开 **http://127.0.0.1:5173**，测试账号见下。

> **局域网/演示请用 `npm run preview`，不要用 `npm run dev`。**
> 实测（限速 30 Mbps、冷缓存）：dev 首次进入页面 **7506 ms / 传输 22.11 MB**
> （element-plus 8.4 MB + echarts 11 MB 未压缩整包），
> 生产产物 **748 ms / 0.78 MB**，约 29 倍差距。
> 代价是改前端代码后必须重新 `npm run build`，preview 不热更新。
>
> **局域网地址会变**：本机 IP 换过多次（`10.51.101.24` → `10.51.174.205` → `10.51.13.169`）。
> 启动后跑 `powershell -ExecutionPolicy Bypass -File .\check-lan.ps1`，
> 它会动态取当前 IP 并打印可直接发给同学的地址，同时验证 8 项（含 CORS 预检与登录）。
> 不要用 `localhost` 判断"能不能用" —— 本机一切正常、外部不可用的情况已经踩过两次。

| 学号 | 密码 | 角色 |
|------|------|------|
| `admin` | `123456` | 管理员 |
| `2024117420` | `123456` | 学生 |
| `2024117421` | `123456` | 学生 |
| `2024117422` | `123456` | 学生 |

---

## 六、已知坑与注意事项

1. **PowerShell 脚本必须纯 ASCII**：中文 Windows 的 PowerShell 5.1 按 GBK 解析无 BOM 的 `.ps1`，
   中文注释会导致解析错位，报出 `Unexpected token ')'` 这类莫名其妙的语法错误。
2. **`-Dfile.encoding` 不要放进 `$env:JAVA_TOOL_OPTIONS`**：PowerShell 会吃掉 `-D` 参数里的点号，
   报 `ClassNotFoundException: /encoding=UTF-8`。用参数数组或启动脚本。
3. **命令行访问 GitHub 报 `schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS`**：
   这是 Windows **系统 TLS（schannel）拿不到客户端凭据**，不是网络不通
   （实测 DNS 正常、TCP 443 可连，唯独 TLS 握手失败）。
   Git for Windows 同时自带 OpenSSL 3.5，可切换后端绕过：
   `git -c http.sslBackend=openssl push`，确认可用后写进配置：
   `git config --global http.sslBackend openssl`。
   这个现象会同时影响 PowerShell / curl / Python（它们都走 schannel 或系统证书），
   但**不影响 JVM**（自带 TLS 栈）—— 这也是本项目早期只能用 Java 写下载器的原因。
4. **`.m2` 与 npm 默认缓存目录在沙箱外不可写**：统一指到工作区内。
5. **不要并发跑多个 Maven 进程**：会造成 `FileSystemException: 另一个程序正在使用此文件`。
6. **Vite 在受限沙箱下报 `spawn EPERM`**：其 Windows 路径解析会 `exec` 子进程。
   在你自己的终端里正常，无需处理。
7. **枚举列一律以枚举名 VARCHAR 存储**，新增枚举值只能追加、不得改名。
8. **首次登录会强制进入新手导引**（`firstLogin=true`），这是 FR-M1-03 的设计行为。
9. **用 PowerShell 的 `HttpWebRequest` 测带 body 的接口会假性 401**：一旦调用
   `GetRequestStream()` 就锁定了请求头，之后再 `Headers.Add('Authorization', ...)` 不生效。
   必须先加头再写 body，或直接用 `Invoke-RestMethod -Headers @{...}`。
   本项目排查 `/api/skills/parse` 时在此浪费了不少时间（公开接口不校验头所以不会暴露）。
10. **Element Plus 的 `el-col` 必须显式给 `:span`**：只写 `:xs="8"` 会同时生成
    `el-col-24`（`flex: 0 0 100%`），它优先级更高且更靠后，会把响应式类覆盖掉导致竖排。
    正确写法：`:span="6" :sm="12" :md="6"`。
11. **DTO 的时间字段必须声明为 `LocalDateTime`，不要用 String**：全局 Jackson 只为
    `LocalDateTime` 注册了 `yyyy-MM-dd HH:mm:ss` 序列化器；声明成 String 并手工
    `toString()` 会产出 ISO 的 `2026-09-17T20:36:49`，前端 `new Date()` 解析失败
    （本项目集市页曾显示 "NaN 天前"）。M4 的三个 VO 已全部改正。
13. **MyBatis-Plus 会覆盖数据库默认值**：insert 会把实体全部字段（含 null）拼进 SQL，
    显式 NULL 会覆盖 DDL 里的 `DEFAULT CURRENT_TIMESTAMP`，报 `Column cannot be null`。
    自动时间戳必须在 `MybatisPlusConfig` 的 MetaObjectHandler 里 fill（已为 occurredAt 配置）。
14. **`Map.of()` 的 `get(null)` 会抛 NPE**（不可变 Map 不接受 null 键）。key 可能为 null 时
    用 HashMap/LinkedHashMap，或先过滤 null。文件列表接口踩过。
15. **注解式 `@Select` 不解析 XML 实体**：写 `&lt;&gt;` 会被原样发给 MySQL 报语法错误，直接写 `<>`。
17. **参与哈希的字段必须可逐字节重现**：MySQL **JSON 列**读写往返会改格式（加空格、重排），
    不能纳入哈希，否则未篡改的记录也会被误报。本项目为此另存 `dim_canonical` 规范串。
18. **Lombok 实体里不要写与字段同名的 `is*()` 方法**：字段 `Integer anonymous` 已有
    `getAnonymous()`，再写 `boolean isAnonymous()` 会构成歧义 getter，
    MyBatis 解析属性时抛 `Illegal overloaded getter method with ambiguous type`。
19. **配额校验不要在"接受邀约"里重复做**：邀约阶段已把该交换置为 `NEGOTIATING` 并占用配额，
    接受时再校验会把这条交换自己也算进 `ongoing`，导致"配额满时永远无法接受"。
21. **有值域的字段必须检查与所有阈值定义是否自洽**：M8 出现"信用值钳在 0~100，
    而等级表要求『优秀 140+』"，导致扣 15 分被吞掉 35 分。这类问题**不抛异常、只是算错**，
    功能测试很难发现，必须写专门的边界测试。
22. **同一列在不同代码里可能有不同存储约定**：`credit_level` 的 DDL 注释、服务层写入、
    注册流程三处理解不一致（1~5 / ordinal 0~4 / 写死 1）。收敛到枚举方法（`levelCode()`）比
    在每处写数字更安全。
23. **用 SQL 造测试数据时必须核对枚举常量名**：我写 `auth_type='STUDENT'`，
    而 `AuthType` 枚举只有 `CAS/OAUTH2/LOCAL`，导致登录 500。造数据前先查枚举定义。20. **新增接口记得同步白名单**：`/api/certificates/**` 是匿名验真入口，
    漏加白名单会让"无需登录即可验真"的设计意图失效（返 401）。16. **用 `Invoke-RestMethod` 测业务错误会误判**：项目约定业务错误返回 HTTP 200 + code，
    而它对 HTTP 200 不抛异常，`try/catch` 会走成功分支。必须显式检查响应体的 `code` 字段
    （我因此一度误报"M5 权限校验失效"，实际服务端一直是对的）。12. **图谱游走跳数别超过 2**：稀疏图里 3 跳链条几乎能把任意两个技能连起来，
    会把明显不相关的需求算成"间接互补"。`MAX_GRAPH_DEPTH = 2`，
    且与 `DemandCardAssembler` 实际只加载一跳邻域的能力相匹配。
24. **`.ps1` 里写中文必须带 UTF-8 BOM，否则会静默损坏整个脚本**（2026-09-22 踩到两次）：
    PowerShell 5.1 在中文 Windows 上按 GBK 解析无 BOM 的 `.ps1`，一个中文字符串
    就能把解析器带偏，报出**指向无关行**的 `Unexpected token` 错误
    （例如报在第 202 行的 `'=' * 66` 上）。更阴的是：加了 BOM 能过，
    但**任何一次重新编辑都可能把 BOM 抹掉**，脚本就又坏了 —— 同一个脚本因此坏了两次。
    结论：能用纯 ASCII 就用纯 ASCII（`check-lan.ps1`、`backup.ps1` 都是）；
    确实需要中文数据时（`verify-graph.ps1` 要校验中文技能名），
    把中文放进独立的 UTF-8 数据文件用 `-Encoding UTF8` 读，
    脚本本身保持 ASCII。**不要**依赖"文件当前能跑"来判断它没问题。
25. **生成的迁移 SQL 必须幂等，且要顺带修正权重**：`zy_skill_ontology` 有唯一键
    `uk_ontology_edge(src,dst,relation_type)`，重复插入会报 1062 并**中断整个脚本**。
    所以 `gen_graph.py` 产出的是 `INSERT ... SELECT ... WHERE NOT EXISTS`，
    后面再跟一条 `UPDATE` 同步 `weight`/`remark` —— 否则改了备注重跑一次毫无变化，
    人会以为脚本没执行。实测连跑三次边数稳定在 593，不增不减。
26. **按权重排序取 TopN 在权重分档少时等于随机取**：`augment()` 原先只按边权重排序，
    而本图谱只有 0.7/0.8/0.9 三档，一个种子技能的邻居几乎全部并列，
    "取前 5"就退化成"任意取 5"。改为按 (被几个种子连到, 权重, ID) 三级排序：
    多种子共同指向是最有效的相关性信号，ID 兜底保证可复现。
27. **脚本里数"度"时要分清有向与无向**：`COMPLEMENT` 是无向边，
    生成器按技能名排序后**只存一次**（方向不固定）。只统计 `src_skill_id`
    会漏掉一半邻域 —— 验收脚本第一版就这么写的，报出"最大度=4"，
    而真实无向图里中心节点度数是 13。统计度数必须 `src UNION ALL dst`。

---

## 七、下一步建议

模块 M1~M9 与 S3 语义引擎均已完成，剩余工作是**补缺口**而非新模块。按优先级：

1. ~~**FR-M9-06 数据备份**（P0）~~ ✅ **已完成** —— `scripts/backup.ps1`，
   备份/校验/恢复/防误覆盖齐备，恢复演练实测通过。
2. ~~**FR-M5-08 阶段性成果独立双向确认**（P1）~~ ✅ **已完成**。
3. ~~**补齐知识图谱关系**~~ ✅ **已完成** —— 3 → 593 条边，跨门类占 36.3%。

**当前优先级最高的三件事**：

1. **前端自动化测试（P1，零覆盖）**：前端目前**一个自动化测试都没有**，
   而缺陷高发区恰好都在前端 —— 本次会话修的 10 个缺陷里 6 个是前端问题
   （雷达图 `undefined`、按钮被遮挡、路由卡顿、进度条……），全部靠手工发现。
   装 Vitest 至少覆盖：`!= null` 判空约定、雷达图数据结构、路由切换、表单校验。
   这是团队接手后最容易持续踩坑的地方。
2. **`zy_skill_course_mapping` 填充（P1）**：表空导致"课程 → 技能"这条
   跨学科推荐链路完全没有数据支撑。
3. **技能本体补到 1000 条（P2）**：当前 788，差 212。
   `gen_ontology.py` + `ontology_supplement*.py` 流程已跑通，属数据填充。

**FR-M4-08 收藏 / 订阅**（P2）仍缺表结构，可放到最后。
5. **FR-M9-05 行为日志采集**：表已建，接消息队列削峰后落盘（S4 阶段）。
6. **Q5 证据锚定**：需先确认可用的可信时间戳/联盟链服务，再动手。
